interface:
  display_name: "GPT Image Relay"
  short_description: "Distribute image prompts across configured relays."
  default_prompt: "Use $gpt-image-2-relay to distribute my ordered image prompts across the configured relay profiles."
